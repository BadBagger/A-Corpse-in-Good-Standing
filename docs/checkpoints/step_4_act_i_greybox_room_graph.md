CHECKPOINT: Step 4 partial - Act I Greybox Quest Flow
GATES:
- Step 1 duel gates: pass, 62 confessions, Registrar opponent coverage intact.
- Step 2 Godot/Popochiu scaffold: pass, project loads under Godot 4.6.3 .NET.
- Step 3 Ink/journal/persistence: pass, Ink compiles and Popochiu save/load roundtrips narrative state.
- Step 4 puzzle dependency graph: pass, `docs/act_i_puzzle_dependency_graph.json` validates 31 unique nodes and 33 valid edges, is acyclic, gates `FL_act_i_complete` directly on the three Rite nodes only, and proves no Rite depends on another Rite.
- Step 4 confession act gate: pass, `tools/Validate-ActIConfessionActGate.ps1` scans Act I room scenes and `ink/prologue.ink` `confession:discover` tags and fails if an Act II/III confession is granted from Act I content.
- Step 4 room graph: pass, 11 registered rooms load, every greybox exit targets a registered Popochiu room, Salt Market exposes hub exits to Old Quay / Harbor Registry / Bone Chandler / Almshouse / Fish Hall / Church of the Drowned, and every registered room is reachable from Mudflats through actual exits.
- Mudflats tutorial hotspots: pass, room graph validation requires `Silt`, `OwnHands`, `HarborView`, `Coat`, `BollardOfTomas`, `MissingBoots`, and `SaltMarketExit`; validates each custom Mudflats handler has explicit look/use/talk/walk/wet responses; Ink bridge validation covers the silt, hands, harbor-view, and coat/wet tutorial knots; and the automated report captures them before the Act I route begins.
- Step 4 quest flow: pass, validator proves the Sabine gate blocks early, Old Quay captures the Tomas hint hub, Appendix B Tomas confessions `cf_pride_list` / `cf_cow_leftroom` / `cf_greed_widows`, silent-bollard foreshadowing, the conditional all-three-bollards Tomas follow-up with `FL_bollard_row_reported`, `IT_flask`, and `FL_flask_taken`, the Salt Market public-recognition beat awards `BorrowedBoots`, sets `FL_market_recognized` / `FL_market_day_hint`, adds `cf_pride_voice`, and now carries the seller face-change, scattered boots, frozen street, and crowd-parting staging that opens the hub, the Salt Market confession queue discovers `cf_cruel_funeral`, the Salt Market whale-oil lamp sets `FL_market_lamp_checked`, the `wet` verb sets `FL_church_sign_wet`, `FL_fish_hall_drain_seen`, `IT_knuckle_salt`, and `FL_registry_lamp_smoked` at the Church sign, Fish Hall drain, Old Quay rope cleat, and Registry desk lamp, Fish Hall awards `IT_coroner_tag`, sets `FL_body_fit_confirmed`, `FL_day_count_proven` / `FL_knows_daycount`, `FL_sabine_absent_from_book`, and discovers `cf_pride_twice`, the Church poor-box discovers `cf_greed_plate` before the Registrar duel, the Church confession booth awards `IT_chit`, sets `FL_church_booth_seen` / `FL_chit_acquired`, and gates Teodor's rate-card stall before booth access, all three Rites can be completed through greybox hotspots, Debt Forgiven proves Prosper blocks before `IT_watch`, captures Bone Chandler / Almshouse room texture flags, expands the Bone Chandler watch trade's fresh-salt-from-a-walking-returned scene and reluctant come-back line without granting `cf_cruel_names` early, and expands Prosper's forgiveness scene without awarding `cf_greed_ring` early, the Registry ledgers beat sets `FL_registry_ledgers_seen`, the Registrar roll-book beat remains scene texture without awarding `cf_pride_handwriting` early, the Registrar blocks before `FL_manifest_known`, the Kestrel ledger blocks before the desk lamp is smoked, `IT_ledger_page` discovers `cf_bt_manifest`, the accepted eight-counter Registrar route spends `cf_bt_manifest` and sets `FL_registrar_sold_manifest`, Borrowed Heartbeat requires both `IT_regulator` and a non-expired `FL_float_warmth_active`, proves the hot pool remains blocked after Juno's table conversation until the rate-card/regulator trade, proves three room transitions expire the warmth, verifies return-to-pool selects `juno_warmth_expired`, the Float staff corner offers `cf_lust_float` and `cf_cow_apologize` while the Name-first critical route correctly leaves `cf_lust_float` locked as opponent-spoken, the Grey Float Juno table captures the authored first pump-governor negotiation, the steam screen sets `FL_float_steam_seen` as art-planning room texture, Teodor's rate-card booth captures the posting panic, the fixed petitioner beats, `FL_kane_seen`, and Kane's day-six pressure line without adding a second confession-spend interface, and `FL_act_i_complete` is reachable.
- Step 4 Rite permutation flow: pass, `tools/godot_validate_act_i_rite_permutations.gd` simulates all six Name/Debt/Heartbeat orderings, proves each Rite can complete before either of the other two, confirms Sabine remains blocked after only one or two Rites, and confirms `FL_act_i_complete` is reachable after the third Rite in every ordering.
- Act I art-coordinate export: pass, `tools/Validate-ActIHotspotMap.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/act_i_hotspot_map.csv` with 69 current Godot scene rows plus `docs/art/act_i_hotspot_overlay.svg` and `docs/art/act_i_hotspot_layout_audit.md`, and validates exits, room counts, coordinate bounds, Registrar duel metadata, wet/blocked Ink knots, the Sabine three-Rite gate, the visual overlay, layout audit markers, and conditional alternate branches such as Tomas's all-three-bollards follow-up.
- Act I background production manifest: pass, `tools/Validate-ActIBackgroundManifest.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/act_i_background_manifest.json` and `docs/art/act_i_background_brief.md` from the hotspot map, validates 11 room contracts at 1920x1080, unique source/export paths, the default y 650-800 walk band, scripted Mudflats navigation, 4 wet-verb art roles, and preserved Registrar duel metadata.
- Act I background asset tracker: pass, `tools/Validate-ActIBackgroundAssetStatus.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/act_i_background_asset_status.csv` and `.md`, validates 44 expected source/export/import slots across 11 rooms, and currently reports 33 present / 11 pending, with all pending rows being final paintover sources rather than missing greybox blockouts.
- Act I Blender blockout tasks: pass, `tools/Validate-ActIBlockoutTasks.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/act_i_blockout_tasks.json` and `.md`, validates 11 room blockout checklists, fixed 1920x1080 side-on camera framing, y 650-800 walk band, 4 wet-verb proof tasks, 4 close-pair review items, and the Registrar duel-format lock.
- Act I background palette audit: pass, `tools/Validate-ActIBackgroundPaletteAudit.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/act_i_background_palette_audit.csv` and `.md`, validates 11 room rows, currently reports 11 audited / 0 pending / 0 failed exports, and enforces G9/G10 for any exported background PNG that exists.
- Corvin animation production contract: pass, `tools/Validate-CorvinAnimationManifest.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/corvin_animation_manifest.json`, `docs/art/corvin_animation_brief.md`, and the Corvin animation asset tracker, validating 3 decay variants, 4 directions, 5 animation sets, 12 fps, deterministic Meshy/Blender-to-sprite-sheet paths, and 129 tracked source/export/import slots currently pending.
- Corvin Meshy source intake: pass, `tools/Validate-CorvinMeshySourceIntake.ps1` is part of the Step 4 gate suite; it imports the standalone Meshy textured GLB into `art/src/characters/corvin/meshy/corvin_act_i_clean.glb` when available, records source hashes in `docs/art/corvin_meshy_source_intake.json`, extracts the biped character output as reference material, maps biped motion candidates to idle/walk/talk/use, and keeps the wet animation marked custom-required.
- Blender Corvin import preflight: pass, `tools/Validate-BlenderCorvinImport.ps1` is part of the Step 4 gate suite; it finds Blender, imports the canonical Corvin Act I GLB headlessly with a 120 second timeout, writes `docs/art/blender_corvin_import_probe.json` and `.md`, verifies the canonical Corvin and shader source blends are present, and keeps source `.blend` files read-only during validation.
- Ink shader still renders: pass, `tools/Validate-InkShaderSpikeStills.ps1` is part of the Step 4 gate suite; it renders the R1 raw side-profile and R2 two-tone ramp still proofs at 1920x1080, writes `docs/art/ink_shader_spike_still_render_status.json` and `.md`, and audits the PNGs through `tools/Test-InkShaderSpikeStillImages.ps1` for nonblank content and palette proximity.
- Ink shader spike contract: pass, `tools/Validate-InkShaderSpikeManifest.ps1` is part of the Step 4 gate suite; it regenerates `docs/art/ink_shader_spike_manifest.json`, `docs/art/ink_shader_spike_brief.md`, and the shader proof tracker, validating the 12 fps / 24-frame yaw-turn stress render, object/world-anchored hatching requirement, screen-space bad control, provisional 6 percent pairwise delta target, first-to-last drift check, locked-palette mapping, no diffusion-per-frame production art, and the Registrar duel-format lock.
- Ink shader yaw render and metrics: pass, `tools/Render-InkShaderSpikeYawSequences.ps1` renders 24 object-anchored candidate frames, 24 screen-space bad-control frames, and 24 locked-palette mapped frames; `tools/Test-InkShaderSpikeMetrics.ps1` writes `docs/art/ink_shader_spike_pairwise_delta.json`, `docs/art/ink_shader_spike_first_last_drift.json`, and `docs/art/ink_shader_spike_metrics_status.md`, currently auditing the object-anchored candidate at 0 percent pairwise max / 0 percent first-last drift against a 28.512 percent bad-control pairwise max.
- Salt Market public-recognition beat: pass, Ink bridge validates `salt_market_public_recognition`; the automated playtest captures the boot seller recognizing Corvin, seller face-change, scattered boots, confession-queue scream, frozen street, six-day arithmetic, crowd parting, `BorrowedBoots`, and market eavesdrop confessions before the Rites begin.
- Salt Market boot-stall aftermath: pass, Ink bridge validates `salt_market_boot_stall_after`; the quest-flow validator proves `BootStall` is blocked before `FL_market_recognized`, then the automated playtest captures the scattered-boots prop cluster, seller avoidance, daughter line, and `FL_boot_stall_after_seen` as an art-planning beat without changing progression.
- Salt Market fishmonger beat: pass, Ink bridge validates `salt_market_fishmonger`; the quest-flow validator proves `cf_cow_drink` is not granted by the public-recognition beat and is instead picked up with `cf_greed_scales` from the fishmonger, while the automated playtest captures the two-scales art anchor, cod/drip exchange, and `FL_fishmonger_seen`.
- Salt Market hub navigation: pass, generated `ToChandler` and `ToAlmshouse` exits now target `BoneChandler` and `Almshouse`; the room graph validator traverses exits from Mudflats and fails if any registered room is unreachable.
- Old Quay support beats: pass, Ink bridge validates `old_quay_tomas`, `old_quay_equipment`, `old_quay_tomas_topics`, `old_quay_silent_bollards`, `old_quay_bollard_petra`, `old_quay_bollard_ledger`, `old_quay_bollard_bride`, `old_quay_bollards_all_seen`, `old_quay_flask`, and `old_quay_rope_cleat`; the automated playtest captures Tomas's returned-rule introduction, the drowning-question exit pressure, the expanded Act I hint hub with memory-decay, Registry duel, Prosper debt, Borrowed Heartbeat, and self-knowledge beats, Appendix B source confessions `cf_pride_list`, `cf_cow_leftroom`, and `cf_greed_widows`, the three individual silent bollards as quay composition/decay foreshadowing, the conditional Tomas follow-up with `FL_bollard_row_reported`, `IT_flask`, `FL_flask_taken`, and the rope-cleat salt pickup.
- Salt Market confession queue: pass, Ink bridge validates `salt_market_confession_queue`; the automated playtest captures the eleven-person paid-truth line, overheard funeral chatter, and `cf_cruel_funeral` discovery before the Registrar duel, with the verified opening Litany UI count now at 19 Act I-legal options.
- Salt Market whale-oil lamp: pass, Ink bridge validates `salt_market_lamp`; the automated playtest captures the amber warmth / no-temperature body read and confirms `FL_market_lamp_checked`.
- Wet verb contract: pass, the HUD exposes `WetButton`, generated interaction hotspots support wet-specific rewards/Ink, hotspot coverage validates look/use/talk/walk/wet responses, and the quest-flow validator proves plain `use` does not set the wet-only Church sign, Fish Hall drain, or Registry lamp flags before `wet` does.
- Fish Hall day-count and emotional proof: pass, Ink bridge validates `fish_hall_ice_table`, `fish_hall_coroner_tag`, `fish_hall_visitor_book`, and `fish_hall_drain`, including the expanded body-table size read, wrong-place tag read, visitor-book pride beat, and drain return line; the automated playtest captures those beats, `IT_coroner_tag`, `FL_body_fit_confirmed`, `FL_knows_daycount`, `FL_sabine_absent_from_book`, `FL_fish_hall_drain_seen`, and `cf_pride_twice`, while the quest-flow validator proves `cf_pride_eulogy` and `cf_cow_didntfight` are not acquired in Act I.
- Church poor-box support: pass, Ink bridge validates `church_poor_box`; the automated playtest captures the bad lock, missing folded notes, Teodor's weak locksmith explanation, and `cf_greed_plate` discovery before the Registrar can lock that sin as opponent-spoken.
- Church stall sign and confession booth access: pass, Ink bridge validates `church_stall_sign`, `church_confession_booth`, and `teodor_needs_chit`; the automated playtest captures the one-shilling paid-truth/eleven-deep queue read as non-progress staging, then captures Teodor blocking the rate-card stall before `IT_chit`, the green-lit paid-truth booth issuing the chit, and `FL_church_booth_seen` / `FL_chit_acquired` without adding the deferred petitioner/confession-spend sequence.
- Registrar narrative beats: pass, Ink bridge validates `registrar_duel_start`, `registrar_duel_win`, and `registrar_duel_loss` are compiled and extract dialogue lines at runtime; `registrar_duel_start` captures the authored Vale-boy pre-duel exchange before the Litany opens, and `registrar_duel_win` now captures the restored-name payoff plus the Registrar selling the Kestrel confession by supper.
- Registrar setup/ledger beats: pass, Ink bridge validates `registry_ledgers`, `registry_roll_book`, `registry_lamp_smoked`, `registry_ledger_blocked`, `registry_ledger_page`, and `registry_registrar_needs_manifest`; the automated playtest captures the ledgers wall, roll-book handwriting texture and scored-paper strike-through without early `cf_pride_handwriting`, blocked ledger, blocked Registrar, lamp distraction, page acquisition, the Registrar seeing the torn corner without rising, and `cf_bt_manifest` discovery before the duel starts.
- Debt Forgiven and Borrowed Heartbeat narrative beats: pass, Ink bridge validates blocked/success beats for the Bone Chandler, Prosper, Juno, and the Harbormaster clerk are compiled and extract runtime dialogue lines, including the Bone Chandler's "No. That's rather the point, isn't it." watch-trade beat, the reluctant "I'd rather you didn't. But come back." line, and Prosper's "I've done that ten thousand times" hand-memory beat.
- Bone Chandler watch gate: pass, Ink bridge validates the expanded `chandler_needs_salt`; the quest-flow validator proves `ProsperWatch` is blocked and `IT_watch` is absent before `IT_knuckle_salt`, then the automated playtest captures the under-glass/watch-chain read before the later fresh-salt trade.
- Almshouse before-watch gate: pass, Ink bridge validates the expanded `prosper_before_watch`; the automated playtest captures Prosper's fresh-every-morning memory-rot line before the watch return, and the quest-flow validator still proves Prosper forgiveness is blocked before `IT_watch`.
- Debt Forgiven room texture: pass, Ink bridge validates `chandler_wares`, `chandler_chess_set`, `almshouse_cots`, and `almshouse_window`; the automated playtest captures those background-design anchors, including the chess-set aging read and Almshouse salt-sheet/harbor-light reads, the quest-flow validator confirms they set `FL_chandler_wares_seen`, `FL_chandler_chess_seen`, `FL_almshouse_cots_seen`, and `FL_almshouse_window_seen`, and `cf_cruel_receipts` / `cf_cruel_names` / `cf_cow_father` / `cf_greed_ring` remain unavailable in Act I.
- Grey Float warmth timing: pass, Ink bridge validates `juno_pool_before_permission`, `juno_hot_pool_soak`, and `juno_warmth_expired`; the quest-flow validator proves the hot pool blocks before Juno permission, remains blocked after the Juno-table negotiation but before the rate-card/regulator trade, the Harbormaster clerk blocks with the regulator but no warmth, the hot-pool soak starts a three-room-transition warmth window and carries amber-screen/steam privacy staging plus a no-sensation body read, wandering three transitions clears `FL_float_warmth_active` and blocks the clerk, return-to-pool plays Juno's authored "walk in a straight line" scolding and restarts warmth, and direct/re-soaked travel to the clerk completes `FL_rite_heartbeat`.
- Grey Float staff corner: pass, Ink bridge validates `float_staff_corner`, including named staff, tip grievances, and paid-silence phrasing; the permutation validator proves StaffCorner discovers `cf_lust_float` when Heartbeat precedes Name, while the Name-first critical route proves StaffCorner cannot reacquire `cf_lust_float` after the Registrar has locked it as opponent-spoken. `cf_cow_apologize` remains discoverable there, and no confession-spend interaction is introduced.
- Grey Float Juno negotiation and art-staging anchors: pass, Ink bridge validates `juno_needs_rate_card`, `float_juno_table`, `juno_regulator_trade`, and `float_steam_screen`; the automated playtest captures the blocked pump-governor setup before the Church rate card, Juno pricing the pump governor at the Church rate card, her long look at the rate card before giving up the regulator, the hard-R steam silhouettes, the heat/shadow/privacy line, `FL_float_juno_table_seen`, and `FL_float_steam_seen` without adding confession spending or changing the locked duel format.
- Harbormaster anteroom and fake-pulse check: pass, Ink bridge validates `harbormaster_checklist_desk`, `harbormaster_sabine_door`, `heartbeat_check_pass`, and `heartbeat_check_fail`; the automated playtest captures the three-box Rite checklist, frosted-glass Sabine door, one-room-away staging, repeatable no-penalty cold failure, and successful wrist check as staged physical action: the clerk takes Corvin's wrist, the regulator ticks under the coat four inches off and slightly too regular, the misplaced pulse is noticed, and pool warmth lets Corvin pass.
- Sabine office close: pass, Ink bridge validates the Act I finale dialogue plus the pen-down first-look beat, water pooling on the floor, Sabine crossing through it, and the extended wrist check; the no-apology validator remains active for Sabine's lines.
- Registrar duel UI readability: pass, the quest-flow validator snapshots the in-Godot duel panel before play and after the first confession, confirming title, accusation count, Salt count, attack metadata, status text, option count, option labels, and close control.
- Act I hotspot fallback coverage and reachability: pass, the room graph validator now calls look/use/talk/walk/wet on every generated Act I hotspot and exit, statically verifies Mudflats custom handlers cover the same verb set, verifies non-empty responses, rejects placeholder/default shipped-copy patterns, requires blocked feedback on gated interactions, and traverses room exits to prove all registered rooms are reachable.
- Automated greybox playtest report: pass, `tools/Record-ActIGreyboxPlaytest.ps1` records the critical Act I route and writes `docs/playtest/results/act_i_greybox_auto_report.md` with room interactions, Ink beats, duel rounds, rewards, flags, and final state.
- Non-linear Rite order gate: pass, `tools/Run-Step4Gates.ps1` now runs the six-order permutation validator after the single-route quest-flow validator.
- Puzzle dependency graph gate: pass, `tools/Run-Step4Gates.ps1` now runs `tools/Validate-ActIPuzzleGraph.ps1` before Godot room execution.
- Confession act gate: pass, `tools/Run-Step4Gates.ps1` now runs `tools/Validate-ActIConfessionActGate.ps1` before Godot room execution.
- Sabine Act I close: pass, `sabine_act_i_audience` is authored in Ink, wired to `SabineDesk`, validated by the Ink bridge, and captured in the automated playtest report through the public Kestrel-confession aftermath, wrist/no-pulse beat, scoped no-Sabine-apology check, and final "Six days" / "Five" exchange.
- Teodor rate-card booth: pass, `teodor_rate_card_booth` is authored in Ink, wired to the Church `RateCard` hotspot, gated by `IT_chit`, validated by the Ink bridge, and captured in the automated playtest report with `FL_teodor_owes`, `FL_rate_card`, `FL_kane_seen`, `cf_cruel_sentences`, Teodor's posting panic, the fixed woman/boy/Kane petitioner beats, Corvin's forced-truth booth line, and Kane's day-six pressure beat. It still does not add a second confession-spend interface.
BLOCKERS:
1. The three Act I Rites are greybox-playable with first-pass authored beats. Remaining before-final-art review items are now enumerated in `docs/playtest/act_i_greybox_remaining_beats.md` instead of carried as an open-ended compression blocker.
2. The Registrar duel now has a readable greybox Litany UI, but it has not been tuned for final animation, audio, visual skinning, or full gamepad/keyboard navigation.
3. The Teodor booth scene is present as an expanded fixed authored beat with the posting panic, three petitioner beats, forced-truth booth line, and Kane's day-six pressure. The fuller booth-choice version, where confession choices could become part of the spend economy, is intentionally deferred so the accepted duel format is not disturbed during room scaffolding.
4. Final room layout and art pass remain out of scope until Act I is playtested in greybox.
DEVIATIONS:
- None to the duel format. The existing Confession Duel data, resolution rules, and tests were preserved.
- Added a generated room scaffold helper for Act I greybox rooms. This is a build convenience only; room logic remains in Godot/Popochiu scripts.
- Added `wet` as a first-class greybox verb because the Act I script identifies Corvin's wetness as a permanent puzzle verb. Current wet wiring is limited to existing Act I wet beats and does not affect the Confession Duel format.
- The Godot duel UI is implemented in GDScript and loads the same JSON duel data rather than importing the standalone C# domain assembly. The C# library remains the test oracle; the Godot validator proves the in-game route matches the accepted Registrar path.
- The Teodor/rate-card scene remains one hotspot interaction for now. It preserves the authored setup, Teodor's posting panic, fixed petitioner beats, and Kane's day-six pressure without adding a second confession-spend interface outside the accepted duel format.
- The Church confession booth now implements the authored `IT_chit` access step and rate-card gate. It still preserves the accepted duel format by deferring the petitioner choice/spend sequence instead of adding a second confession-spend interface.
- The Church poor-box beat is modeled as a normal room pickup, not a confession-spend scene. That preserves the locked duel format while still feeding the Litany economy.
- The Salt Market queue and Float staff corner are modeled as normal overheard pickups. They preserve the locked duel format while adding authored Act I-safe Litany sources.
- The Grey Float Juno table now carries the authored first pump-governor negotiation instead of remaining room texture only. It preserves Borrowed Heartbeat gating by keeping the hot pool locked until the rate-card/regulator trade.
- The Grey Float bilge regulator no longer grants `cf_lust_float`; StaffCorner is the authored eavesdrop source, and global opponent-spoken lockout can still prevent acquisition if the Registrar says that confession first.
- Grey Float warmth expiry is implemented as narrative-state room-transition timing. The authored Juno return-comment beat is present as `juno_warmth_expired`; the current greybox proves the mechanical fail/pass rule at the Harbormaster clerk and the return-to-pool re-soak path.
- The current greybox makes `FL_manifest_known` a hard gate for starting the Registrar duel. The script labels `IT_ledger_page` as optional but strongly incentivized; this stricter prototype gate prevents the accepted Registrar route from opening without its required BETRAYAL counter.
- `FL_registrar_sold_manifest` is set on Registrar duel victory as an Act II/III consequence hook from the Act I script. It does not alter the accepted duel route or current Act I completion gate.
- `BorrowedBoots` remains the current Popochiu item id for the script's `IT_boots` beat, preserving the existing prologue inventory registration instead of introducing a duplicate boots item.
- `cf_pride_eulogy` and `cf_cow_didntfight` are not awarded at Fish Hall in Act I. The Act I room document mentions them there, but the Litany source of truth marks them Act II and Act III, and G15 forbids early acquisition.
- `cf_cruel_receipts`, `cf_cruel_names`, `cf_cow_father`, `cf_greed_ring`, and `cf_pride_handwriting` are not awarded from Bone Chandler / Almshouse / Registry room texture in Act I. The Act I room document mentions them there, but the Litany source of truth marks them Act II, and G15 forbids early acquisition.
NEXT:
Run `tools/Start-ActIHumanPlaytest.ps1 -RefreshAutomatedReport` for one human Act I greybox playtest against `docs/playtest/act_i_human_greybox_playtest.md` and `docs/playtest/act_i_greybox_remaining_beats.md`, then make line-level pacing and prop-readability fixes before final art.
